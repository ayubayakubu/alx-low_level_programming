#include "main.h"

/**
 * print_most_numbers - print numbet from 0 to 9
 * without 2 and 4
 * Return: Always 0
 */
void print_most_numbers(void)
{
	int b = 0;

	while (b < 10)
	{
		if (b != 2 && b != 4)
			_putchar(a + '0');
		a++;
	}
	_putchar('\n');
}

